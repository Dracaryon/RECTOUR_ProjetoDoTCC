<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="telas.mobitour.pretcc.com.telas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TelaMainActivity"
            android:theme="@style/APPThemeNOActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Telalogin" />
        <activity android:name=".Mapa_Dexcricao" />
        <activity android:name=".Tela_MainActivity" />
        <activity android:name=".TelaHistorias" />
        <activity android:name=".TelaHistoriasDetalheActivity"></activity>
    </application>

</manifest>